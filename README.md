# Chatbot_MCP
POC of a streamlit Chatbot with MCP tools (sqlite3 and graphic drawing)

## Installation

```
uv sync
mv .streamlit/example.secrets.toml .stremlit/secrets.toml
vim .streamlit/secrets.toml
```